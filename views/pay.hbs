<div class="container">
    <div class="row">
        <div class="col-sm-12 mt-5">
            {{#if user}}
            <h1>DEPOSIT</h1>
            <p>Your steamid: {{#each user}}{{this.steamid}}{{/each}}</p>
            <form action="" method="post">

            </form>
            {{/if}}
        </div>
    </div>
</div>